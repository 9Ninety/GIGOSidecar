{
  "name": "gigo-sidecar",
  "private": true,
  "type": "module",
  "engines": {
    "bun": ">=1.0.0"
  },
  "scripts": {
    "start": "bun run src/server.ts",
    "test:sse": "bun run tests/sse-test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "es-toolkit": "^1.45.1"
  },
  "devDependencies": {
    "@types/bun": "^1.2.9",
    "typescript": "^6.0.2"
  }
}
